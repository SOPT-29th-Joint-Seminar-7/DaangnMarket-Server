/**
 *  @포스트 전체 조회
 *  @route GET post/
 *  @access public
 */

const postAllController = async (req, res) => {
  try {
  } catch (err) {}
};

const postController = {
  postAllController,
};

export default postController;
